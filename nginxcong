server {
    listen 80;
    server_name services.nirvarnah.com;
    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl;
    server_name services.nirvarnah.com;

    ssl_certificate /etc/letsencrypt/live/services.nirvarnah.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/services.nirvarnah.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:3000; # Your second app port
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}


server {
    listen 80;
    server_name ajala.tonyicon.com.ng;
    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl;
    server_name ajala.tonyicon.com.ng;

    ssl_certificate /etc/letsencrypt/live/ajala.tonyicon.com.ng/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ajala.tonyicon.com.ng/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:3002; 
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}



server {
    listen 443 ssl;
    server_name cwu.tonyicon.com.ng;

    ssl_certificate /etc/letsencrypt/live/cwu.tonyicon.com.ng/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/cwu.tonyicon.com.ng/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    location / {
        proxy_pass http://localhost:5003; 
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}